import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFC000",
    },
    secondary: {
      main: "#6D8EAD",
    },
    error: {
      main: "#FF6600",
    },
    success: {
      main: "#6D8EAD",
    },
  },
});

export default theme;
